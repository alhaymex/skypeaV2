// import { BlogPages as BlogPageType } from "@/db/schema";

// interface BlogPageProps {
//   page: BlogPageType;
//   isFirstPage: boolean;
// }

// export default function BlogPage({ page, isFirstPage }: BlogPageProps) {
//   return (
//     <article className={`mb-12 ${isFirstPage ? "" : "mt-12 border-t pt-12"}`}>
//       <h2 className="text-3xl font-semibold mb-4">{page.title}</h2>
//       <div
//         className="prose max-w-none"
//         dangerouslySetInnerHTML={{ __html: page.content }}
//       />
//     </article>
//   );
// }

import React from "react";

const BlogPage = () => {
  return <div>BlogPage</div>;
};

export default BlogPage;
